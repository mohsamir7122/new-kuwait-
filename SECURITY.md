# Security

لا تُحفظ مفاتيح أو كلمات مرور أو بيانات سوق خاصة داخل المستودع. ملفات `runtime/`
و`artifacts/` مستبعدة من Git. لا يجوز للشفرة تجاوز `robots.txt` أو تسجيل الدخول
أو `CAPTCHA` أو القيود التعاقدية للمصادر.

إذا ظهر سر في Commit أو Artifact، يجب إبطال السر أولًا ثم إزالة أثره بطريقة
منسقة؛ حذف الملف من آخر Commit وحده لا يكفي.
